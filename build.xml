<?xml version="1.0" encoding="utf-8" ?>
<project name="Charba" default="build" basedir="." xmlns:ivy="antlib:org.apache.ivy.ant">

	<!--
        Apache IVY ANT lib include
    -->
	<taskdef resource="org/apache/ivy/ant/antlib.xml" uri="antlib:org.apache.ivy.ant" />

	<!-- 
		CHARBA project configuration 
	-->
	<property name="charba.version" value="1.0" />
	<property name="charba.built.by" value="www.pepstock.org" />
	<tstamp>
		<format property="TODAY" pattern="yyyy-MM-dd HH:mm:ss" />
	</tstamp>
	<property name="src.dir" value="src" />
	<property name="dest.dir" value="classes" />
	<property name="dist.dir" value="dist" />
	<property name="module.archive.file" value="${dist.dir}/charba.jar" />
	<property name="temp.dir" value="temp" />
	<property name="javadoc.dir" value="javadoc" />
	<property name="sources.archive.file" value="${dist.dir}/charba-sources.jar" />
	<property name="javadoc.archive.file" value="${dist.dir}/charba-javadoc.jar" />

	<property name="ivy.pubrevision" value="20180103-SNAPSHOT" />
	<!-- 
		JAVA compiler options 
	-->
	<property name="javac.debug" value="true" />
	<property name="javac.debuglevel" value="lines,vars,source" />
	<property name="javac.source" value="1.7" />
	<property name="javac.target" value="1.7" />
	<property name="javac.includeantruntime" value="false" />
	<property name="javac.nowarn" value="true" />

	<target name="clean" description="Cleans folders">
		<mkdir dir="${dest.dir}"/>
		<mkdir dir="${dist.dir}"/>
		<mkdir dir="${javadoc.dir}"/>
		<delete failonerror="false" includeemptydirs="true">
			<fileset dir="${dist.dir}" includes="**/*"/>
			<fileset dir="${dest.dir}" includes="**/*"/>
			<fileset dir="${javadoc.dir}" includes="**/*"/>
		</delete>
	</target>

	<!--
        Apache IVY
    -->
	<target name="ivyRetrieve">
		<ivy:retrieve sync="true" pattern="lib-ivy/[artifact]-[revision](-[classifier]).[ext]" pathid="ivyPath" />
	</target>

	<!--
	  Mapping confs to scopes is important, otherwise
	  unmapped confs are included as optional. If you
	  have private confs, the best option seems to
	  be marking them as provided or system. See
	  IVY-1201 for an ehancement request.
	-->
	<target name="makePom">
		<ivy:makepom ivyfile="ivy.xml" pomfile="charba.pom">
			<mapping conf="default" scope="compile" />
		</ivy:makepom>
	</target>

	<target name="ivyPublish" depends="build">
		<ivy:resolve />
		<ivy:publish resolver="local" pubrevision="${revision}">
			<artifacts pattern="${dist.dir}/[artifact].[ext]" />
		</ivy:publish>
	</target>

	<target name="ivyPublish-snapshot" depends="makePom, build">
		<ivy:publish resolver="nexus-snapshots" pubrevision="${revision}-SNAPSHOT" forcedeliver="true" overwrite="true" publishivy="false">
			<artifacts pattern="${dist.dir}/[artifact].[ext]" />
		</ivy:publish>
	</target>

	<target name="ivyPublish-release" depends="makePom, build">
		<ivy:publish resolver="nexus-releases" pubrevision="${revision}" forcedeliver="true" overwrite="true" publishivy="false">
			<artifacts pattern="${dist.dir}/[artifact].[ext]" />
		</ivy:publish>
	</target>

	<target name="buildSources">
		<!-- build the sources jar -->
		<jar jarfile="${sources.archive.file}">
			<fileset dir="${src.dir}" />
		</jar>
	</target>

	<target name="buildJavadoc" depends="javadoc">
		<!-- build the javadoc jar -->
		<jar jarfile="${javadoc.archive.file}">
			<fileset dir="${javadoc.dir}" />
			<manifest>
				<section name="Charba">
					<attribute name="Version" value="${charba.version}" />
					<attribute name="Built-By" value="${charba.built.by}" />
					<attribute name="Creation-time" value="${TODAY}" />
				</section>
			</manifest>
		</jar>
	</target>

	<target name="compile" description="Compile Charba" depends="clean, ivyRetrieve">
		<javac srcdir="${src.dir}" includes="**" encoding="utf-8" destdir="${dest.dir}" includeantruntime="${javac.includeantruntime}" source="${javac.source}" target="${javac.target}" nowarn="${javac.nowarn}" debug="${javac.debug}" debuglevel="${javac.debuglevel}">
			<classpath refid="ivyPath"/>
		</javac>
		<copy todir="${dest.dir}">
			<fileset dir="${src.dir}"/>
		</copy>
	</target>

	<target name="build" description="Build Charba distribution" depends="compile">
		<!-- Creates the JAR file adding the classes folder and adding the mnifest part -->
		<jar jarfile="${module.archive.file}">
			<fileset dir="${dest.dir}"/>
			<manifest>
				<section name="Charba">
					<attribute name="Version" value="${charba.version}" />
					<attribute name="Built-By" value="${charba.built.by}" />
					<attribute name="Creation-time" value="${TODAY}" />
				</section>
			</manifest>
		</jar>
	</target>

	<target name="javadoc" depends="build">
		<javadoc destdir="${javadoc.dir}"
				useexternalfile="yes"
		         author="true"
		         version="true"
		         use="true"
		         windowtitle="Charba, GWT wrapper for CHART.JS">
			<classpath>
				<path refid="ivyPath"/>
				<fileset dir="${dest.dir}"/>
				<pathelement path="${java.class.path}" />
			</classpath>
			<packageset dir="${src.dir}" defaultexcludes="yes">
				<include name="org/pepstock/**"/>
			</packageset>
		</javadoc>
	</target>
</project>